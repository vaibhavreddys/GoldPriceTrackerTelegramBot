# Core bot library — [job-queue] extra is REQUIRED for /subscribe and /alert features.
# Without it, application.job_queue will be None and scheduled jobs won't run.
python-telegram-bot[job-queue]==21.9

# Web scraping
flask==3.1.0
cloudscraper==1.2.71
beautifulsoup4==4.12.3

# Production WSGI server (replaces Flask dev server)
gunicorn==23.0.0
